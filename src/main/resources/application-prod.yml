spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/znalfxpt?characterEncoding=utf8&useSSL=false&serverTimezone=CTT
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
